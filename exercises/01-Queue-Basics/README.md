## 01 - Queue Basics

Create a `Queue` class with:

- `enqueue(value)` to insert at the end.
- `dequeue()` to remove from the front.

Use an internal list to store elements.

## Required variables

- `queue`: instance of `Queue`.
- You must enqueue `10` and `20`.
- `first_out`: result of the first `dequeue()`.
