## 03 - Safe Dequeue

Modify `dequeue()` so it does not fail when the queue is empty.

If there are no elements, it must return `None`.

## Required variables

- `empty_queue`: empty queue.
- `result_on_empty`: result of `dequeue()` on an empty queue.
